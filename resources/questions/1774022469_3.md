# Python Quiz Question
    
    ## Question
    [QUESTION]  
Consider the following Python code that uses a metaclass to dynamically add attributes to a class:

```python
class Meta(type):
    def __new__(cls, name, bases, dct):
        dct['dynamic_attr'] = 'I am dynamic'
        return super().__new__(cls, name, bases, dct)

class MyClass(metaclass=Meta):
    existing_attr = 42

obj = MyClass()
print(obj.dynamic_attr)
```

What is the output of `print(obj.dynamic_attr)`? 

A) AttributeError  
B) 'I am dynamic'  
C) None  
D) 42
    
    ## Answer
    B) 'I am dynamic'
    
    ---
    *Generated on: 2026-03-20 16:01:09*
    